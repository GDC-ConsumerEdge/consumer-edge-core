
---

# Installs Anthos Bare Metal on all inventory
- hosts: workers
  become: true
  become_user: root
  roles:
    - validate
    - google-tools
    - update
    - ready-ubuntu
    - setup-kvm
    - abm-install
    - abm-software
    - abm-login-token
  vars: []
  tags:
    - hardware
