
---

# Installs Anthos Bare Metal on inventory of "workers"
# must specify `--tags never` on the CLI to run this
- hosts: workers
  become: true
  become_user: root
  roles:
    - abm-remove
  vars: []