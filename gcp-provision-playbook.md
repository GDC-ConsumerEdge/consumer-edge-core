# Create GCP Provisioning Playbook

## Goal
Create a new Ansible playbook `all-gcp-provision.yml` for cloud-only provisioning and verify its syntax.

## Tasks
- [x] Task 1: Create `all-gcp-provision.yml` with the specified content → Verify: File exists in root
- [x] Task 2: Verify playbook syntax → Verify: `ansible-playbook --syntax-check all-gcp-provision.yml` passes
- [x] Task 3: Commit the new playbook → Verify: `git status` shows clean and commit exists

## Done When
- [x] `all-gcp-provision.yml` is created and committed.
- [x] Playbook passes syntax check.
