
- hosts: workers
  roles:
    - abm-login-token
  vars:
    kubeconfig_shared_location: "/var/kubeconfig/kubeconfig"
  tags:
    - hardware

- hosts: cloud_type_abm
  roles:
    - abm-login-token
  vars:
    kubeconfig_shared_location: "/var/kubeconfig/kubeconfig"
  tags:
    - cloud
